import React from "react";
import { ScrollView, Text } from "react-native";

const PlanDayHeader = () => {
  return (
    <ScrollView showsVerticalScrollIndicator={false} className="mt-8">
      <Text>Helo</Text>
    </ScrollView>
  );
};

export default PlanDayHeader;
